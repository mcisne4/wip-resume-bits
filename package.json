{
  "name": "resume-bits",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "dev-env": "cd packages/env && bun dev",
    "dev-logger": "cd packages/logger && bun run dev"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "npm-run-all2": "^6.2.0"
  }
}
