{
  "name": "resume-bits",
  "private": true,
  "scripts": {
    "dev-env": "cd packages/env && LOG=false bun run --watch src/index.ts",
    "dev-logger": "cd packages/logger && bun run --watch src/index.ts"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "npm-run-all2": "^6.2.0"
  }
}
